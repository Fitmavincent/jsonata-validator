// Valid JSONata expressions
$.firstName
$.address.city
$.products[price > 100]
$.orders[status = "shipped"].total ~> $sum()

// Invalid JSONata expressions (for testing validation)
$.firstName..invalid
$.products[price > 100
$$invalid.syntax
$count(invalid syntax here
